# Deutschland — Regierungsbezirke (`vg25_rbz`)

BKG **VG25**, Layer `vg25_rbz`. Extraktion: `bun run bkg:extract -- --area de-regierungsbezirke`.

OSM-Export für Regierungsbezirke (`admin_level=5` wo gemappt) → `source/osm.fgb`.
