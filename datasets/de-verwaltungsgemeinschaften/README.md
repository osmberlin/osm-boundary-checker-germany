# Deutschland — Verwaltungsgemeinschaften (`vg25_vwg`)

BKG **VG25**, Layer `vg25_vwg`. Extraktion: `bun run bkg:extract -- --area de-verwaltungsgemeinschaften`.

OSM-Abbildung ist je nach Region unterschiedlich; Export und `matchProperty` projektspezifisch wählen.
