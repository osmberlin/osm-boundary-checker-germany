/** Repo-root generated index of dataset slugs (see `generateAreasJson.ts`). */
export const AREAS_GEN_BASENAME = 'areas.gen.json'

/** URL path at site root (dev, preview, static deploy). */
export const AREAS_GEN_URL_PATH = `/${AREAS_GEN_BASENAME}`
