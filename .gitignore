node_modules/
**/node_modules/

.DS_Store
dist/
*.log
.env
.env.local
# Large downloaded artifacts at repo root only (not `report/public/data/` for static assets).
/data/
!report/src/data/
!report/src/data/**
report/src/data/reviewQueue.gen.ts

# Transient tippecanoe input (removed after successful run; ignore if left behind)
**/output/_build/

# TEMP until we figure out how to store them
*.pmtiles

# Rust sidecar build artifacts
rust/**/target/

# Generated local runtime artifacts for CI validation
.artifact-runtime/
.artifact-runtime-report/
